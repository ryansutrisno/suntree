import AdminShellPage from '@/Pages/admin/shell-page';
import type {AdminShellProps} from '@/Pages/admin/shell-page';

export default function UsersIndex(props: AdminShellProps) {
    return <AdminShellPage {...props} />;
}
